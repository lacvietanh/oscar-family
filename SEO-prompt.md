dùng model mạnh nhất (5.1 codex) bảo nó:
---
SEO Task:
install & import @vueuse/head then refactor all router to implement `useHead`.
Make sure maximum SEO efficiently follow these requirements:
- use `useHead` with all meta tag
- JSON-LD script use `innerHTML` instead of `children` avoid char escape.